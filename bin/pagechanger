#! /usr/bin/env ruby

#require "rubygems"
require "thor"
require "pagechanger"

class PagechangerCommand < Thor
  method_option :recursive => false, :type => :boolean, :aliases => "-r", :desc => "Recursively alter files."
  
  desc "process CONFIG DIR", "Mass alter text files with Reg ex expressions."
  
  def process config_filename, dir
  
  end

end

PagechangerCommand.start